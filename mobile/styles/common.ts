import Colors from "./colors";
import styles, {defaultTestStyles, spStyles} from "./styles";
import { strings } from "./strings";

export default {
    strings : strings,
    styles : spStyles,
    colors : Colors,
    defaultTestStyles: defaultTestStyles
}